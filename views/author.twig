{% extends 'layout/default.twig' %}

{% block content %}
	<h1>{{ author.name }}</h1>
	{% for post in posts %}
		{% include ['_partials/tease-'~post.post_type~''.twig'', '_partials/tease.twig'] %}
	{% endfor %}
{% endblock %}
